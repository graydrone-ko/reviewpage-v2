{
  "name": "reviewpage-platform",
  "version": "1.0.0",
  "description": "ReviewPage survey platform with integrated Express + React deployment",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm install --legacy-peer-deps && npm run build && cp -r build/* ../backend/public/",
    "build:backend": "cd backend && npm install --legacy-peer-deps && npm run build",
    "start": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}